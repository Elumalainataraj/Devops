##### Added from below details
### <https://artifacthub.io/packages/helm/bitnami/kafka> ###
livenessProbe:
  initialDelaySeconds: 60

resources:
  requests:
    cpu: "100m"
    memory: 256Mi
  limits:
    cpu: "1000m"
    memory: 256Mi

persistence:
  enabled: false

zookeeper:
  persistence:
    enabled: false

  resources:
    requests:
      cpu: "100m"
      memory: 256Mi
    limits:
      cpu: "1000m"
      memory: 256Mi
