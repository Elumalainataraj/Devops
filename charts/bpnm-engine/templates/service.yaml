apiVersion: v1
kind: Service
metadata:
  name: {{ include "bpnm-engine.fullname" . }}
  labels:
    {{- include "bpnm-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bpnm-engine.selectorLabels" . | nindent 4 }}
