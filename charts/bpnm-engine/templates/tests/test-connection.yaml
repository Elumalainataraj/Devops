apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bpnm-engine.fullname" . }}-test-connection"
  labels:
    {{- include "bpnm-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bpnm-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
