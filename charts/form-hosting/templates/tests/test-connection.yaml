apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "form-hosting.fullname" . }}-test-connection"
  labels:
    {{- include "form-hosting.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "form-hosting.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
