apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bpmn-engine.fullname" . }}
  labels:
    {{- include "bpmn-engine.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "bpmn-engine.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- if eq .Chart.AppVersion "lts" }}
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
    {{- end }}
      labels:
        {{- include "bpmn-engine.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bpmn-engine.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          {{- range $name, $value := .Values.env.open }}
              {{- if not (empty $value) }}
            - name: {{ $name | quote }}
              value: {{ $value | quote }}
              {{- end }}
          {{- end }}
          {{- range $key, $secrets := .Values.secrets }}
            - name: {{ $key | quote }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secrets.secretKeyRef | quote }}
                  key: {{ $secrets.key | quote }}
          {{- end }}
          ports:
            - name: http
              containerPort: 8080  ## Exposed port from container
              protocol: TCP
          ## TODO, will add this check later stage
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
