apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bpmn-engine.fullname" . }}-test-connection"
  labels:
    {{- include "bpmn-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bpmn-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
