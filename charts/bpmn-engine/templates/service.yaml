apiVersion: v1
kind: Service
metadata:
  name: {{ include "bpmn-engine.fullname" . }}
  labels:
    {{- include "bpmn-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bpmn-engine.selectorLabels" . | nindent 4 }}
